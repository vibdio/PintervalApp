2025-12-24  グレースケール切替文言のフォント・サイズ・色を他のラベルと統一（public/index.html, styles.css）
2025-12-24  グレースケール切替UIを「グレースケールを使用」テキスト（左寄せ）＋トグルスイッチ（右端）に変更。「グレー」「カラー」の文言を削除。（public/index.html）
2025-12-21  未ログイン時はページ表示時に自動でログインページへ遷移する機能を追加（public/app.js）
2025-12-21  カウントダウンバーの色が残量に応じて白→黄→赤に変化するよう改善（public/app.js）
2025-12-21  スタートボタン押下時に左カラムを自動で隠すよう改善（public/app.js）
2025-12-21  前回選択した表示間隔（秒）をlocalStorageで記憶・復元する機能を追加（public/app.js）
2025-12-14  右カラムにカウントダウン残量を示す白色の縦バーを追加。残り時間に応じて高さが変化し、インターバル・表示中ともに進捗を視覚化。
2025-12-14  インターバル（gap）時は右カウントダウンバーを非表示に変更。
2025-12-14  カウントダウンバーが無段階で滑らかに減少するように変更。
更新履歴.txt
2025-12-13  縦長（モバイル/狭幅）表示時に左/右カラムをハンバーガーメニュー（ドロワー）へ格納し、基本はビューワのみ表示するよう改善。左右は排他的に開閉し、タッチ環境でも操作ボタンを常時表示。
2025-12-13  一時停止/再生でカウントダウンが最初から始まる不具合を修正（paused 状態で残り時間を保持して再開）。
2025-12-14  表示間隔（秒）のデフォルト値を60に変更（index.htmlの<select id="interval">）。
2025-12-14  表示モードのデフォルトを「ランダム」に変更（index.htmlの<select id="order">）。
2025-12-14  表示間隔の初期値が60になるようapp.jsの初期化処理を修正。
2025-12-13  「すべてのピン」で0件になる問題を修正。/api/me/pins を各ボードの pins 集約方式に変更し、bookmark によるページング取得にも対応（最大500件）。
2025-12-13  ビューワーの背景色をページ全体の背景色（--bg）に統一し、色の不一致を解消。
2025-12-13  一時停止（pause）中も左カラムの操作を無効化するよう修正。
2025-12-11  テーマ検索機能を廃止。UI・ロジック・仕様書から関連要素を削除。
2025-12-08 03:19:51 UTC
- 初回リリース: 仕様書(Pinterval_仕様書.txt)に基づき、Webアプリ「Pinterval」を新規作成。
- 開発方針.txtに基づくイベントバス（phase/priority/lock/Abort対応）を導入。
- 左/中央/右カラムの3カラムレイアウト、スタンバイ/プレイの状態管理、カウントダウン・履歴サムネイル・操作ボタン（再生/一時停止・停止・次へ）を実装。
- /api/search エンドポイント（Pinterest検索のプロキシ）。pidgets API を優先し、失敗時はフォールバック・モックに切替。


2025-12-09 00:00:00 UTC
- Pinterest API v5 対応: /api/search を Pinterest 公式 REST API v5 `search/pins` エンドポイント呼び出しに変更。旧 pidgets API と HTML スクレイピングのフォールバックを廃止。
- API キーの扱いを仕様書どおり環境変数 `PINTEREST_ACCESS_TOKEN` 前提に整理。`USE_MOCK=1` でモック画像のみを返すモードを維持。
- 再生履歴を Web ストレージ（localStorage）に保存するよう変更。ページ再読み込み後も右カラムのサムネイルから閲覧可能。
- README を現行仕様（Pinterest API v5 / 環境変数 / モックモード）に合わせて更新。
